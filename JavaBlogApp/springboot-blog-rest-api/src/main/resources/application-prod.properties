# Configure url, username and password
spring.datasource.url = jdbc:mysql://javaprojects.cywwaxkcneot.eu-west-2.rds.amazonaws.com:3306/myblog?useSSL=false&serverTimezone=UTC
spring.datasource.username = root
spring.datasource.password = rootpassword

# Configure hibernate properties
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

# Configure hibernate dll auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

server.port=5000